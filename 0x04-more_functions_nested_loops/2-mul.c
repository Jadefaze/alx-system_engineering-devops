#include "main.h"
/**
 *  mul - to check whether upper or not
 *
 * @a: the number to multiply
 * @b: the second number to multiply
 * Return: product of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
