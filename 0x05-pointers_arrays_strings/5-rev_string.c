#include "main.h"

/**
 * rev_string - print string in reverse
 *
 * @s: the string to print
 *
 * Return: reversed string
 */

void rev_string(char *s)
{
	/*find length */

	int i;
	int l;
	int k;

	i = 0;
	while (*(s + i) != '\0')
	{
		i++;
	}

	/*using the length - i */

	while (	
}

