#include "main.h"

/**
 * _Print_rev_recursion - reverse print a string
 * @s: the string to to print
 * Return: nothing
 */

void _print_rev_recursion(char *s)
{
	if (*s == '\0')
	{
		if (*(s - 1))
		{
			_putchar(*(s - 1));
			--s;
		}
		return;
	}
	_print_rev_recursion(s);
}
